<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itour.dao.FeedbackDao">
	<!-- Result Map -->
	<resultMap id="BaseResultMap" type="com.itour.entity.Feedback">
		<result column="id" property="id" />
		<result column="create_time" property="createTime" />
		<result column="status" property="status" />
		<result column="update_time" property="updateTime" />
		<result column="title" property="title" />
		<result column="content" property="content" />
		<result column="customer_id" property="customerId" />
		<result column="result" property="result" />
		<result column="customerName" property="customerName"/>
		<result column="email" property="email"/>
		<result column="mobile" property="mobile"/>
	</resultMap>

	<!-- feedback table all fields -->
	<sql id="Base_Column_List">
		id, create_time, status, update_time, title, content, customer_id, result
	</sql>
	<sql id="Extend_Column_List">
		id, create_time, status, update_time, title, content, customer_id, result,customerName,email,mobile
	</sql>

	<!-- 查询条件 -->
	<sql id="Example_Where_Clause">
		where 1=1
		<trim suffixOverrides=",">
			<if test="id != null and id != ''">
				and id like CONCAT('%', #{id}, '%')
			</if>
			<if test="createTime != null ">
	  <![CDATA[  and  DATE_FORMAT(feedback.create_time,'%Y-%m-%d')  = DATE_FORMAT( #{createTime},'%Y-%m-%d') ]]>
			</if>
			<if test="status != null ">
				and status = #{status}
			</if>
			<if test="updateTime != null">
	 <![CDATA[  and  DATE_FORMAT(feedback.update_time,'%Y-%m-%d')  = DATE_FORMAT( #{updateTime},'%Y-%m-%d') ]]>
			</if>
			<if test="title != null and title != ''">
				and title like CONCAT('%', #{title}, '%')
			</if>
			<if test="content != null and content != ''">
				and content like CONCAT('%', #{content}, '%')
			</if>
			<if test="customerId != null and customerId != ''">
				and customer_id like CONCAT('%', #{customerId}, '%')
			</if>
			<if test="result != null and result != ''">
				and result  like CONCAT('%', #{result}, '%')
			</if>
		</trim>
	</sql>


	<!-- 插入记录 -->
	<insert id="add" parameterType="com.itour.entity.Feedback">
		<!-- <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id"> 
			SELECT LAST_INSERT_ID() </selectKey> -->
		insert into
		feedback(id,create_time,status,update_time,title,content,customer_id,result)
		values(#{id},now(),#{status},now(),#{title},#{content},#{customerId},#{result})
	</insert>

	<!-- 修改记录，只修改只不为空的字段 -->
	<update id="update" parameterType="com.itour.entity.Feedback">
		update feedback set update_time=now(),
		<trim suffixOverrides=",">
			<if test="status != null  ">
				status=#{status},
			</if>
			<if test="title != null and title != '' ">
				title=#{title},
			</if>
			<if test="content != null and content != '' ">
				content=#{content},
			</if>
			<if test="customerId != null and customerId != '' ">
				customer_id=#{customerId},
			</if>
			<if test="result != null and result != '' ">
				result=#{result},
			</if>
		</trim>
		where id=#{id}
	</update>

	<!-- 删除记录 -->
	<delete id="delete" parameterType="java.lang.String">
		delete from feedback where id = #{id}
	</delete>

	<!-- 根据id查询 客户反馈 -->
	<select id="queryById" resultMap="BaseResultMap" parameterType="java.lang.String">
		select
		feedback.id, feedback.create_time, feedback.status, feedback.update_time, feedback.title, feedback.content, feedback.customer_id, feedback.result,customers.email email,customers.mobile mobile,customers.customer_name customerName 
		from feedback left join customers on feedback.customer_id = customers.id where feedback.id = #{id}  
	</select>

	<!-- 客户反馈 列表总数 -->
	<select id="queryByCount" resultType="java.lang.Integer"
		parameterType="com.itour.base.page.BasePage">
		select count(1) from feedback
		<include refid="Example_Where_Clause" />
	</select>

	<!-- 查询客户反馈列表 -->
	<select id="queryByList" resultMap="BaseResultMap"
		parameterType="com.itour.base.page.BasePage">
		select
		feedback.id, feedback.create_time, feedback.status, feedback.update_time, feedback.title, feedback.content, feedback.customer_id, feedback.result,customers.email email,customers.mobile mobile,customers.customer_name customerName 
		from feedback left join customers on feedback.customer_id = customers.id 
		<include refid="Example_Where_Clause" />
		<if test="pager.orderCondition != null and pager.orderCondition != ''">
			${pager.orderCondition}
		</if>
		<if
			test="pager.mysqlQueryCondition != null and pager.mysqlQueryCondition != ''">
			${pager.mysqlQueryCondition}
		</if>
	</select>
</mapper>   
